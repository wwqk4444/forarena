# 选择题模板（已记住，生成题目一律按此格式）

> 配套样例文件：`templates/quiz.example.json`（纯 JSON，无注释，可直接喂给程序）

## JSON 结构

```jsonc
[
  {
    "question": "中国的首都是哪座城市？",
    "options": ["上海", "北京", "广州", "深圳"],
    "answer": "B",
    "score": 5,
    "explanation": "北京是中国的首都"
  },
  {
    "question": "下列哪些是直辖市？",
    "options": ["北京", "上海", "杭州", "天津"],
    "answer": "ABD",
    "score": 5,
    "explanation": "北京、上海、天津是直辖市，杭州不是"
  }
]
```

## 字段说明

- 顶层是**数组**，数组中每个对象对应一道选择题。
- `question`：题干。
- `options`：选项列表，按数组顺序依次对应 A / B / C / D …（不写字母，由顺序推断）。
- `answer`：答案字母。
  - 单选：一个字母，如 `"B"`。
  - 多选：多个字母连写、按字母序，如 `"ABD"`。
- `score`：分值，**可选**，默认 5；若整份试卷都不写 `score`，则按 100 分均分。
- `explanation`：答案解析，**可选**；公布成绩后学生在回顾页可见。

## 填空题

- **去掉 `options` 字段**（不要给空数组，直接不写）。
- `answer` 直接写答案文本：
  - 多个可接受写法用 `|` 分隔，如 `"维C|维生素C|抗坏血酸"`（判分时任一写法命中即为对）。
  - 以 `*` 开头表示**包含即对**，如 `"*光合作用"`（学生答案里包含该关键词就算对）。
  - 两者可组合：`"*维生素C|*抗坏血酸"`。
- `score` / `explanation` 规则同上（可选）。

## 输出约定

1. 只输出 JSON 数组本体，不加 ```json 代码围栏之外的解释文字（除非用户要说明）。
2. 中文内容原样输出，不做 Unicode 转义。
3. 选项数量默认 4 个（A–D），用户另有要求时从其要求。
4. 不确定 `score` 时宁可省略该字段，让其走默认/均分逻辑。
5. 若题目为多选，题干中需明示「多选」或「哪些」，避免与单选混淆。
