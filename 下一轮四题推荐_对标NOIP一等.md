# 下一轮四题推荐 · 对标 NOIP 一等奖

> 生成日期：2026-09-21 ｜ 目标：**上海 NOIP 一等奖**（等级线对标见第一节）
> 选题原则：全部**沿用已设计方案中的题**（SET8–12 / SET13–22 已核实题池），只做「对标一等线」的校准与组卷
> 查重：四题与已考 20 场（0701S–0912S）+ 补充 SET1–6 + 备选池**无重复**（逐题已核，见第七节）
> 所有题号已联网核实 ✅（见第七节来源）

---

## 一、标定：上海一等线到底要多少分？

| 年份 | 全国一等基准 | **上海一等线** | 上海一等奖人数 | 卷面难度 |
|---|---|---|---|---|
| NOIP 2024 | 200 / 400 | **216 / 400** | 76 人 | 常规梯度 |
| NOIP 2025 | 137 / 400 | **148 / 400** | 77 人（曹二 7 人进前 31） | 「一黄一紫两黑」 |

**赛制**：4 题 × 100 分 = 400，**4.5 小时**（8:30–13:00），机试。

### 关键结论

1. **一等不是「会做难题」，而是「T1 稳拿 + T2 咬住 + T3/T4 抢暴力」**。
   按 2024 的 216 线拆解：T1 满分 100 + T2 满分 100 = 200，**还差 16 分——这 16 分只能从 T3/T4 的部分分里出**。
2. 2025 年线暴跌到 148，是因为卷面变难（两黑），**头部学生被 T3/T4 卡住，分数线整体下移**。
   这意味着：**只要 T1/T2 不丢分，148 的线几乎自动到手；能不能上 216，全看 T3/T4 的部分分功底。**
3. 这恰好命中你们自己的判断——《对标NOIP_知识缺口清单》结尾那句：
   > 「知识缺口之外，**部分分意识**仍是最大的分数缺口。」

### 本套卷的目标分数结构

| 档位（能力排序） | 目标总分 | T1 | T2 | T3 | T4 |
|---|---|---|---|---|---|
| S 档（潘家文／沈永承／陈子昂／史如茗…） | **300–400** | 100 | 100 | 60–100 | 40–100 |
| A 档（陈子琚／汤景行／朱鸿／徐德屹…） | **216–300** | 100 | 70–100 | 30–60 | 10–30 |
| B 档（沈徽／忻士云／钟箫谦…） | 120–215 | 90–100 | 40–80 | 10–30 | 0–10 |
| C/D 档 | 60–120 | 70–100 | 20–50 | 0–15 | 0 |

> **A 档达标算式**：`100（T1）+ 80（T2）+ 30（T3）+ 10（T4）≈ 220` → 正好压过一等线。
> 所以本卷的设计铁律是：**每道题都必须留一条「不动脑也能拿」的部分分路线**，尤其是 T3/T4。

---

## 二、主推方案 A：一等达标卷（四个 P0/P1 缺口，一题一个）

> 挑题逻辑：缺口清单的四个 P0 是**堆、倍增、线段树、KMP**。堆已由 0912S（W1 贪心+堆）补掉，
> **剩下三个 P0 加一个 P1（二分答案）正好四个题位**——用一套卷把 NOIP 前最该堵的洞一次性堵完。

| 位 | 题目 | 来源 | 洛谷 | 难度 | 核心考点 | 堵的洞 |
|:--:|---|---|---|:--:|---|---|
| **T1** | 循环交换 `Swapity Swapity Swap` | USACO 2020 February Silver P1 | **[P6148](https://www.luogu.com.cn/problem/P6148)** | ★★ | 置换的幂 + 倍增/快速幂 | 🔴 **倍增 P0** |
| **T2** | 虫洞排序 `Wormhole Sort` | USACO 2020 January Silver P2 | **[P6004](https://www.luogu.com.cn/problem/P6004)** | ★★☆ | 二分答案 + 并查集连通判定 | 🟡 **二分答案 P1** |
| **T3** | 密文 `KRIPTOGRAM` | COCI 2011/2012 Contest 4 Task 6 | **[P8085](https://www.luogu.com.cn/problem/P8085)** | ★★★ | 广义/单词级 KMP（相对位置编码 + 失配特判） | 🔴 **KMP P0** |
| **T4** | 最小化干草堆 `Minimizing Haybales` | USACO 2022 January Platinum P1 | **[P8099](https://www.luogu.com.cn/problem/P8099)** | ★★★★ | 偏序建模 + 线段树懒标记区间加 + 字典序贪心 | 🔴 **线段树 P0** |

难度阶梯：★★ → ★★☆ → ★★★ → ★★★★，与 NOIP「T1 基础 / T2·T3 中档 / T4 难」的官方梯度描述完全对齐。

---

## 三、逐题详解

### T1 · 循环交换（P6148，USACO 2020 Feb Silver P1）

- **题意**：初始序列 `1..N`，一轮 = 依次对 M 个区间做反转；共执行 K 轮。求最终序列。
  `N ≤ 1e5, M ≤ 100, K ≤ 1e9`。
- **正解**：① 先把「一轮 M 次反转」预处理成一个置换 `f`，复杂度 `O(N·M)`；
  ② K 轮 = 置换的 K 次幂，用**倍增/快速幂** `f[i][j] = f[i-1][f[i-1][j]]` 做到 `O(N log K)`；
  或找循环节 `O(N)`。
- **为什么放 T1**：**这是全队「倍增 P0」的第一道赛题**。缺口清单原话——「倍增只隐含出现在 cowland 的 LCA 中，ST 表、倍增跳点无训练」。
  置换幂是倍增思想最干净的载体（没有复杂背景干扰），放在 T1 让**所有人先学会「把重复操作打包成 2^k」**。
- **部分分路线**：`K ≤ 1000` 的测试点直接暴力模拟 `O(N·M·K)`；建议手动切一档 `K ≤ 1e5`（暴力 + 预处理）作中间档。
- **预期**：全队满分率应 ≥ 80%（本题是地基，不允许大面积丢分）。**若本题满分率 < 60%，说明倍增课没上透。**
- **教学前置**：一节课（置换的概念 + 倍增/快速幂思想）。这是 SET13 T1，原本就定位「倍增先修」。

### T2 · 虫洞排序（P6004，USACO 2020 January Silver P2）

- **题意**：N 头牛错位排列，M 条虫洞（宽度 w），求「可用的最小虫洞宽度的最大值」；已排好输出 −1。
- **正解**：二分宽度 `W` → 把 `w ≥ W` 的虫洞抓进并查集 → 检查每个错位位置 `i` 与 `p[i]` 是否同集合。
  也可免二分：按宽度**降序**加边（Kruskal 变式），首个「全部归位」时刻即为答案。
- **为什么放 T2**：二分答案在缺口清单里是「🟡 接触过 1 题，**无专项**」，而它是 NOIP 出现率极高的技巧。
  本题的判定函数是「并查集连通性」——队内并查集 ✅（charges 76% 满分、reveg、closing），
  **把新技巧（二分）架在已掌握的工具（并查集）上**，是二等升一等的标准台阶。
- **部分分路线**：`N, M` 小的测试点直接 `O(N·M)` 暴力枚举每个宽度。
- **坑点（讲评重点）**：① 只判「错位对连通」而不是「全图连通」；② 已排好的 −1 特判；
  ③ 答案一定出自某个虫洞的宽度，二分的边界别写成 `1..1e9`。
- **预期**：A 档 60–100；B 档 30–60。

### T3 · 密文（P8085，COCI 2011/2012 #4 Task 6）

- **题意**：明文与密文都是单词序列（总长 ≤ 1e6），密文 = 明文某子串做**单词替换加密**（一个密文词对应一个明文词）。求密文在明文中最早出现的位置。
- **正解**：把单词编码成「**与上一次出现位置的距离**」（首次出现记 `inf`），然后跑**修改版 KMP**——
  模式串某词首次出现且该词在文本窗口内出现过时要特判失配（否则误判）。
- **为什么放 T3**：**KMP 是全队零训练的 P0**。若不先用一道真题把它钉死，
  「学了模板但不会用」会一直拖到 NOIP 卷面上。本题是 KMP 思想的**正宗应用题**（不是裸模板），
  且失配特判那一步非常考理解——正好用来区分「背了模板」和「懂了 next 数组」。
- **部分分路线**：小数据逐位置暴力匹配（`O(|明文|·|密文|)` 可过小档）；COCI 官方子任务制，直接沿用官方数据分档。
- **坑点**：经典反例 `c a a b a b c` vs `x x y x y z`——窗口外出现的词会让裸 KMP 误判。
- **预期**：S 档 60–100；A 档 20–50；**其余人必须拿到暴力档**（这是 T3 的及格线）。
- **教学前置**：KMP 课必须先上（8 周计划 W3）。

### T4 · 最小化干草堆（P8099，USACO 2022 January Platinum P1）

- **题意**：N 堆干草，相邻两堆高度差 ≤ K 才能交换，求可达的**字典序最小**序列。`N ≤ 1e5, h ≤ 1e9`。
- **正解**：① **建模**：`|h_i − h_j| > K` 的相邻对相对顺序被**锁死** → 问题变成「带固定偏序的字典序最小拓扑序」；
  ② 值域离散化后，用**线段树维护每堆被多少堆挡住**，每次取「阻挡数为 0 的最小值」，删除时做**区间加懒标记**。
- **为什么放 T4**：**线段树 + 懒标记是全队最大的结构性缺口**（零专项），而这个缺口的正确打法是
  「不是先学模板，而是在一道必须用它的题里被逼出来」。本题第一步建模（想不出就全卷崩盘）是 AK 杀手的正确形态，
  但**部分分极其友好**——这正是我们要练的「拿该拿的分」。
- **部分分路线（官方，题面已标明）**：
  - 10%：`N ≤ 100` → 直接 `O(N²)` 暴力找「当前可移动到最前的最小值」；
  - 另有 20%：`N ≤ 5000` → `O(N²)` 贪心可过；
  - 其余 70%：需要线段树懒标记 `O(N log N)`。
  > **即：不做正解也能拿 30 分**——这就是本卷「一等靠部分分」的核心题。
- **讲评可说**：treap / 分块也是正解（不必强求线段树一种写法）。
- **预期**：S 档 40–100；A 档 10–30；**B 档及以下应至少拿到 10–30 的暴力分**。
- **教学前置**：线段树 + 懒标记课（8 周计划 W2）。⚠️ 见第五节的开考前提。

---

## 四、换题备选池（同题位可替换，全部来自已设计方案）

| 位 | 备选 1 | 备选 2 | 备选 3 |
|:--:|---|---|---|
| T1 | **P6145** [时间线 Timeline](https://www.luogu.com.cn/problem/P6145)（USACO20FEB Gold P1，拓扑/DAG 最长路，★★） | **P7557** Acowdemia（二分 + h 指数贪心，★★） | **P6004**（与 T2 对调位置亦可） |
| T2 | **P6145** Timeline G（同上，作 T2 略易） | **P6148**（若倍增课未上，把 T1 换成它当 T2） | — |
| T3 | **P4656** 回文划分（CEOI 2017，双向贪心 + 滚动哈希，★★★） | **P7152** 奶牛基因（USACO20DEC Gold，字符串计数 DP，★★★☆） | **P4652** 单行道（Tarjan，**已考过 0823S，勿重复**） |
| T4 | **P4416** 床单 Plahte（COCI 2017/18 #1 P6，扫描线 + 标记永久化 + 启发式合并，★★★☆） | **P6280** 锻炼 Exercise（USACO20OPEN Gold P3，置换环 LCM + 质数幂背包，★★★★） | **P8099**（保持原位） |

> 注：若本场**必须避开任何未授课的知识点**，请把 T1/T2/T3/T4 全部换成「已掌握知识点」的题
> （拓扑 ✅ / 并查集 ✅ / 树状数组 ✅ / 单调栈 ✅ / 最短路 ✅ / 数论 ✅ / 区间DP ✅），
> 组卷思路改为「纯达标检验卷」而不是「堵洞卷」——**但这会白白浪费最该堵的四个洞，不推荐。**

---

## 五、开考前提（必须先确认）

⚠️ **本卷四题全部依赖尚未开课的知识点**，这是刻意的（堵洞卷的本质），但必须满足前置条件：

| 题 | 前置课 | 8 周计划位置 |
|---|---|---|
| T1 倍增 | 置换 + 倍增/快速幂 | 并入 W2 树算法巩固 / 独立一节 |
| T2 二分答案 | 二分答案判定套路 | **W4** |
| T3 KMP | KMP 课 | **W3** |
| T4 线段树懒标记 | 线段树 + 懒标记 | **W2** |

**结论：本卷最早应在 W4（二分答案课）之后开考。** 若按 8 周计划推进，W1 已由 0912S 落地，
则本卷可安排在 **W4 结束后的第一个整场**，作为「一等达标检验卷」。
（若时间紧，可先砍到三题：T1+T3+T4 = 倍增 / KMP / 线段树，二分留到 W4 单独考。）

---

## 六、组卷、判分与赛前提醒

### 6.1 判分：用 `.cdf` 的逐点 fullScore 落地子任务

**USACO 题（T1/T2/T4）官方数据是逐点给分，不含清晰子任务档**，而本卷的命脉是部分分意识。

好在你们的判卷配置（如 `cdf文件/0912S.cdf`）**本身就是逐点 `fullScore`**——
每个测试点单独给分，`0-x` 作样例点（`fullScore: 0`）、`1-x / 2-x / 3-x` 按前缀分组。
**这意味着不需要改判卷器，只要在 `.cdf` 里改每点的 `fullScore` 就能做出子任务档。**

建议本卷的分档（总分 100）：

| 题 | 档 1（暴力/小数据） | 档 2（中等） | 档 3（满解） |
|---|---|---|---|
| T1 循环交换 | `K ≤ 1000` 的测试点，30 分 | — | 其余 70 分 |
| T2 虫洞排序 | `N, M ≤ 500` 的测试点，30 分 | — | 其余 70 分 |
| T3 密文 | 沿用 COCI 官方数据分档（原题即子任务制） | | |
| T4 最小化干草堆 | `N ≤ 100`，10 分 | `N ≤ 5000`，20 分 | 其余 70 分 |

落地要点：
1. **文件名前缀即档位**（`1-*` = 档 1、`2-*` = 档 2、`3-*` = 档 3），前缀内的点给同一个 `fullScore`；
2. 每点 `fullScore` 之和 = 100（USACO 原始数据点较多，别直接照搬点数）；
3. `timeLimit` 保持 1000ms、`memoryLimit` 256MB，`compilerConfiguration` 用 `g++ C++14 O2`（与 0912S 一致）；
4. ⚠️ **务必在题面第一屏就写出子任务表**——否则「有分可拿」这件事学生看不到，等于没有。

### 6.2 数据获取

| 题 | 数据包 |
|---|---|
| P6148 | `https://usaco.org/current/data/feb20_silver.zip` |
| P6004 | `https://usaco.org/current/data/jan20_silver.zip` |
| P8085 | COCI 官方存档 `hsin.hr/coci/archive/2011_2012/` Contest 4 |
| P8099 | `https://usaco.org/current/data/jan22_platinum.zip` |

> 前两场（feb20_silver / jan20_silver）**不在原方案已列的下载清单里**，需新增。

### 6.3 时间分配建议（按 NOIP 4.5 小时制，赛前口头强调）

```
0:00–0:40  T1  必须一次过，写完立刻对拍
0:40–1:50  T2  二分边界与判定函数，写完手造小样例验证
1:50–3:20  T3  先写暴力拿部分分，再冲正解（不要空着）
3:20–4:10  T4  只做部分分/暴力，除非已有明确正解思路
4:10–4:30  回查：文件名、空文件夹、int 溢出、大样例
```

### 6.4 一等线真正的杀手是非算法失分

翻你们自己的赛后记录，丢掉的分**大多不是不会**：

| 历史事故 | 丢分 |
|---|---|
| 空文件夹交卷 | 单科 0 |
| 一个冒号三题编译错误 | 3 分 |
| `cowland7.in` 忘改文件名 | 0 分 |
| int 溢出 | 一次丢满分，一次丢 31 分 |
| 硬编码 / 手贴 2779 个 YES/NO | 诚信问题 |

**考前最后一句话建议**：「T3/T4 就算不会正解，暴力分必须交上去；文件名和大样例比最后一问更值钱。」

> 顺带一条：`cdf文件/0912S.cdf` 里已经能看到 `compileState`（编译状态）和逐点 `message`（错误信息）——
> **赛后可以直接用 `.cdf` 统计「编译失败率」「T1 满分率」「暴力档得分率」**，比翻成绩单快得多。
> 建议本卷考完后，按这三项回填《赛后记录》。

---

## 七、核实与查重记录

| 题 | 题号核实 | 与已考 20 场查重 | 与备用/已设计方案查重 |
|---|---|---|---|
| P6148 循环交换 | ✅ 洛谷题解页确认 `[USACO20FEB] Swapity Swapity Swap S` | 无重复 | SET13 T1（本批启用） |
| P6004 虫洞排序 | ✅ 洛谷题解页确认 `[USACO20JAN] Wormhole Sort S` | 无重复 | SET10 T2（本批启用） |
| P8085 密文 | ✅ 洛谷题面确认 `[COCI2011-2012#4] KRIPTOGRAM` | 无重复 | SET9 T2（本批启用） |
| P8099 最小化干草堆 | ✅ 洛谷题面确认 `[USACO22JAN] Minimizing Haybales P` | 无重复 | SET8 T4（本批启用） |
| P6145 时间线（备选） | ✅ 洛谷题单确认 `[USACO20FEB] Timeline G` | 无重复 | SET21 T1 — **若本批用 T1，需从 SET21 撤下** |

⚠️ 另提示：`五套选题方案_SET8-12.md` 里 SET8 T2「干草分配」标注洛谷 **P9982**，本次搜索**仍未核实到该题号**
（洛谷 USACO-G 题单确认有 `[USACO23DEC] Haybale Distribution G`，但题号未能从公开来源确认）。
该题也在本卷的备选视野内，**若考虑启用，请先在洛谷站内按英文名复核**，并注意：
**主流解法是「三分 + 前缀和/二分」而非线段树加权中位数**，作为「线段树周」的题略偏题（见下方说明）。

---

*一句话总结：这套卷不追求「四题都难」，而是**用四个题位把堆以外的三个 P0 加二分一次性堵上**，
同时用 T3/T4 的官方子任务把「部分分意识」变成肌肉记忆——**216 分线的构成是 100+80+30+10，不是四题各 54 分。***
